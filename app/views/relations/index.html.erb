<h1>Listing relations</h1>

<table>
  <thead>
    <tr>
      <th>Good count</th>
      <th>Bad count</th>
      <th>Similarity</th>
      <th>Movie</th>
      <th>Movie</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @relations.each do |relation| %>
      <tr>
        <td><%= relation.good_count %></td>
        <td><%= relation.bad_count %></td>
        <td><%= relation.similarity %></td>
        <td><%= relation.movie %></td>
        <td><%= relation.movie %></td>
        <td><%= relation.user %></td>
        <td><%= link_to 'Show', relation %></td>
        <td><%= link_to 'Edit', edit_relation_path(relation) %></td>
        <td><%= link_to 'Destroy', relation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Relation', new_relation_path %>
